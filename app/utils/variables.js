export const gameOptions = Object.freeze({
    "playerVsPlayer": "player vs player",
    "playerVsComputer": "player vs computer"
});

export const resetOptions = Object.freeze({
    "normal": "normal",
    "all": "all"
})